{
  "name": "server",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "elysia": "catalog:",
    "@elysiajs/cors": "^1.3.3",
    "@elysiajs/node": "^1.3.1",
    "@trpc/server": "catalog:",
    "@elysiajs/trpc": "^1.1.0",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "zod": "catalog:",
    "@ciaran/api": "workspace:*",
    "@ciaran/auth": "workspace:*",
    "@ciaran/db": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "tsx": "^4.19.2",
    "@types/node": "catalog:",
    "tsdown": "catalog:",
    "@ciaran/config": "workspace:*"
  }
}
